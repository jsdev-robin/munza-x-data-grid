import { Input } from './components/ui/Input';

const App = () => {
  return (
    <div
      style={{
        width: '800px',
        margin: 'auto',
      }}
    >
      <Input />
    </div>
  );
};

export default App;
